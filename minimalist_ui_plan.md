# Minimalist UI Plan

## Goals

- Make UI "beautiful" and "minimalistic".
- Shrink heart icon.
- Make UI smaller.

## Implementation

1. **LikeButton**: Fix `redis-500` typo. (Already resized to `size-6`).
2. **PostSection**:
   - Max Width: `max-w-md`.
   - Spacing: `gap-4`.
   - No heavy borders.
   - Avatar `size-8`.
   - Text `text-sm` / `text-xs`.
